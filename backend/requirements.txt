# Python recomendado: 3.11 o 3.12
# Core y Servidor
python-dotenv>=1.0.1
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart    # Súper necesario si FastAPI va a recibir imágenes de la cámara

# Base de datos
supabase>=2.8.1,<2.10.0

# Visión / Captura (VERSIÓN HEADLESS PARA SERVIDORES)
opencv-python-headless>=4.9.0.80

# IA y Modelos
google-genai>=1.0.0,<1.3.0
ultralytics>=8.0.0  # Para que YOLOv8 funcione

# Misma familia que exigen supabase y gotrue/postgrest
httpx>=0.26,<0.28